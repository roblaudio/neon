--!native
--!optimize 2
--!nocheck
--!nolint
-- Client generated by Neon v0.0.0 (https://github.com/roblaudio/neon)

return table.freeze({
	HelloWorld = table.freeze({
		fire = function() end,
	}),
	CanMigrate = table.freeze({
		invoke = function() end,
	}),
})
